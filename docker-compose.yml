services:
  designer-studio:
    build:
      context: .
      args:
        NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-http://localhost:3000}
    ports:
      - "3002:3002"
    environment:
      PORT: 3002
      HOSTNAME: 0.0.0.0
    restart: unless-stopped
